import LoginCard from "@/components/loginCard";
import React from "react";

const login = () => {
  return (
    <div className="flex h-screen w-screen items-center justify-center max-w-screen-md p-4 m-auto">
      <LoginCard />
    </div>
  );
};

export default login;
